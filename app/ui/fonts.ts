import { DynaPuff, Inter } from 'next/font/google';

export const inter = Inter({
    weight: ['400', '600', '700'],
    subsets: ['latin']
});
export const dynaPuff = DynaPuff({
    weight: ['400', '600', '700'],
    subsets: ['latin']
});
